/** Placeholder until 1.20 adds the rows. */
export function HomePage() {
  return (
    <section>
      <h1>Home</h1>
      <p className="muted" data-testid="home-empty">
        Nothing here yet. Add a library in Settings and scan it.
      </p>
    </section>
  );
}
